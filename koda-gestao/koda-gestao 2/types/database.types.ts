// Placeholder — substitua rodando `npm run db:types` (com Supabase CLI) ou
// exportando os types pelo painel do Supabase (Database → Generate types).
// Enquanto isso, usamos `any` para não travar o build.

export type Database = any;
